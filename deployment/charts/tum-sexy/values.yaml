namespace: latest
tag: main
url: "app.tum.sexy"

# see https://github.com/bitnami/charts/tree/main/bitnami/mariadb-galera for more options, the se are just the most important ones
mariadb-galera:
  enabled: true
  rootUser:
    password: changeme-changeme-changeme
    forcePassword: true
  db:
    username: backend
    password: changeme-changeme-changeme
    forcePassword: true
    database: tca
  galera:
    mariabackup:
      password: changeme-changeme-changeme
      forcePassword: true
  tls:
    enabled: true
    autoGenerated: true
  persistence:
    size: 5Gi
    storageClass: local-path

# see https://github.com/bitnami/charts/tree/main/bitnami/mariadb for more options, the se are just the most important ones
mariadb:
  enabled: false
  architecture: standalone
  auth:
    rootPassword: changeme-changeme-changeme
    username: backend
    password: changeme-changeme-changeme
    replicationPassword: changeme-changeme-changeme
    forcePassword: true
    database: tca
  primary:
    pdb:
      create: true
  volumePermissions:
    enabled: true
  persistence:
    size: 5Gi
    storageClass: local-path


backend:
  sentry:
    dsn: changeme-changeme-changeme
  apns:
    auth_key: changeme-changeme-changeme
    key_id: changeme-changeme-changeme
    team_id: changeme-changeme-changeme